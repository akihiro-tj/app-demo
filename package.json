{
  "name": "app-demo",
  "private": true,
  "scripts": {
    "build": "turbo run test && turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "check": "turbo run check",
    "check:write": "turbo run check:write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "turbo": "^2.4.2",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
