{
  "name": "world-history-map",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json && && biome check src",
    "check:write": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json && biome check --write src"
  },
  "devDependencies": {
    "@app-demo/biome-config": "workspace:*",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "@tsconfig/svelte": "^5.0.4",
    "svelte": "^5.28.1",
    "svelte-check": "^4.1.6",
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
